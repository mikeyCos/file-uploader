<!-- <form action="/drive/files/upload" onsubmit="onSubmit(event)" method="POST" onclick="stopPropagation(event)" enctype="multipart/form-data" class="form-upload-files" data-form-type="files">
  <h2>Upload File(s)</h2>
  <ul>
    <li class="form-item">
      <label for="upload_files">Upload file(s)</label>
      <input type="file" name="upload_files" onchange="uploadFiles_onChange(event)" id="upload_files" multiple>
      <% if (locals.errors?.upload_files) { %>
        <p class="input-error active-error"><%= errors?.upload_files.msg %></p>
      <% } %>
    </li>
  </ul>
  <button type="button" onclick="closeDialog()">Cancel</button>
  <button type="submit" disabled>Upload</button>
</form> -->

<!-- onsubmit="onSubmit(event)" -->

<form action="/drive/files/upload" method="dialog" onsubmit="onSubmit(event)" onclick="stopPropagation(event)" enctype="multipart/form-data" class="form-upload-files" data-form-type="files">
  <h2>Upload File(s)</h2>
  <ul>
    <li class="form-item">
      <label for="upload_files">Upload file(s)</label>
      <input type="file" name="upload_files" onchange="uploadFiles_onChange(event)" id="upload_files" multiple>
      <% if (locals.errors?.upload_files) { %>
        <p class="input-error active-error"><%= errors?.upload_files.msg %></p>
      <% } %>
    </li>
  </ul>
  <button type="button" onclick="closeDialog()">Cancel</button>
  <button type="submit" disabled>Upload</button>
</form>